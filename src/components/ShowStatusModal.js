import React, {Fragment} from 'react';
import {ModalHeader,ModalBody} from 'reactstrap';

const ShowStatusModal = () => {
    return(
        <Fragment>
            <ModalHeader>機台運作情況</ModalHeader>
            <ModalBody>

            </ModalBody>
        </Fragment>
    );
}

export default ShowStatusModal;